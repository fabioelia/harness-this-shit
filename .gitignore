# dependencies
node_modules/
.pnp
.pnp.js

# builds
dist/
build/
*.tsbuildinfo

# local data / runtime
*.db
*.db-shm
*.db-wal
*.log

# env
.env
.env.local
.env.*.local

# editor / os
.DS_Store
.vscode/
.idea/

# yarn PnP / berry artifacts (project uses npm)
.pnp.*
.yarn/
yarn.lock
app/.pnp.*
app/.yarn/
app/yarn.lock

# routine memory store (local)
app/server/memory/
app/server/switchboard.db*

# harness runtime artifacts (per-deployment, not source)
harness/examples/routines/.harness
harness/examples/routines/state/

# the app's routines folder is seeded at boot and is per-deployment
app/server/routines/
.secrets.json
